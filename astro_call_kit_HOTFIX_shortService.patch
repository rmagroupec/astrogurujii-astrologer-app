diff --git a/packages/astro_call_kit/android/src/main/AndroidManifest.xml b/packages/astro_call_kit/android/src/main/AndroidManifest.xml
index 226d12c..3fc7c04 100644
--- a/packages/astro_call_kit/android/src/main/AndroidManifest.xml
+++ b/packages/astro_call_kit/android/src/main/AndroidManifest.xml
@@ -5,15 +5,23 @@
       AndroidManifest.xml — Gradle's manifest merger folds this into the
       final app manifest automatically, so no manual edits to the app
       module are required. All permissions this service needs
-      (FOREGROUND_SERVICE, FOREGROUND_SERVICE_PHONE_CALL, WAKE_LOCK,
-      VIBRATE, USE_FULL_SCREEN_INTENT, POST_NOTIFICATIONS) are already
-      declared in the app's own manifest.
+      (FOREGROUND_SERVICE, WAKE_LOCK, VIBRATE, USE_FULL_SCREEN_INTENT,
+      POST_NOTIFICATIONS) are already declared in the app's own manifest.
+
+      foregroundServiceType is "shortService", NOT "phoneCall". The
+      phoneCall type is gated on Android 14+ to apps that are the default
+      dialer or hold MANAGE_OWN_CALLS + ConnectionService — this app is
+      neither, so that type made startForeground() fail on every modern
+      device, which was the actual root cause of the ringtone never
+      playing. shortService requires no special role/permission and is
+      built for exactly this: a brief (<3 min), self-terminating
+      foreground task.
     -->
     <application>
         <service
             android:name="com.astrologer.vaidikguru.astrocallkit.CallRingtoneService"
             android:exported="false"
-            android:foregroundServiceType="phoneCall" />
+            android:foregroundServiceType="shortService" />
     </application>
 
 </manifest>
diff --git a/packages/astro_call_kit/android/src/main/kotlin/com/astrologer/vaidikguru/astrocallkit/CallRingtoneService.kt b/packages/astro_call_kit/android/src/main/kotlin/com/astrologer/vaidikguru/astrocallkit/CallRingtoneService.kt
index 8302741..b47ea3f 100644
--- a/packages/astro_call_kit/android/src/main/kotlin/com/astrologer/vaidikguru/astrocallkit/CallRingtoneService.kt
+++ b/packages/astro_call_kit/android/src/main/kotlin/com/astrologer/vaidikguru/astrocallkit/CallRingtoneService.kt
@@ -164,12 +164,28 @@ class CallRingtoneService : Service() {
         val id = if (notifId > 0) notifId else FALLBACK_NOTIF_ID
         val notification = adoptedOrFallbackNotification(notifId, title, body)
         try {
-            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q) {
+            // IMPORTANT: this is NOT FOREGROUND_SERVICE_TYPE_PHONE_CALL.
+            // That type is gated by Android 14+ to apps that are the
+            // default dialer OR hold MANAGE_OWN_CALLS + use
+            // ConnectionService — neither of which applies here. Declaring
+            // or requesting that type without meeting those conditions
+            // makes startForeground() fail every time on Android 14+,
+            // which is exactly why ringing silently never started even
+            // though everything else was wired correctly.
+            //
+            // "shortService" (Android 14+) is the correct type for a
+            // brief, self-terminating task like this: no dialer role, no
+            // special permission beyond the base FOREGROUND_SERVICE
+            // already declared, capped at ~3 minutes — comfortably above
+            // our 45s ring timeout. Pre-Android 14 devices don't enforce
+            // (or even have) foreground service types at all, so they just
+            // get a plain startForeground() call.
+            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.UPSIDE_DOWN_CAKE) {
                 ServiceCompat.startForeground(
                     this,
                     id,
                     notification,
-                    ServiceInfo.FOREGROUND_SERVICE_TYPE_PHONE_CALL,
+                    ServiceInfo.FOREGROUND_SERVICE_TYPE_SHORT_SERVICE,
                 )
             } else {
                 startForeground(id, notification)
